spring:
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
    database-platform: com.example.jpa_postgresql.framework.config.CustomDialectConfig
    open-in-view: false